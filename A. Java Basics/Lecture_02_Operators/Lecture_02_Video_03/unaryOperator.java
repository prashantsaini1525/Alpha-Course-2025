
public class unaryOperator {
    public static void main(String[] args) {
        
        //Pre Increment

        // int a = 10;
        // int b = ++a; //(a= a + 1)
        
        // System.out.println("First Increment the value of a and then Value of a : " + a);
        // System.out.println("And then We Use Increment Value of a and then Value of b : " + b);

        //Post Increment

        // int a = 10;
        // int b = a++; //(a= a + 1)
        
        // System.out.println("And then Increment of a and then value of a :" + a);
        // System.out.println("First Use Value of a and then value of b : " + b);

        //Pre Decrement

        // int a = 10;
        // int b = --a; //(a= a - 1)
        
        // System.out.println("First Decrement the value of a and then Value of a : " + a);
        // System.out.println("And then We Use Decrement Value of a and then Value of b : " + b);
        
        //Post Decrement

        int a = 10;
        int b = a--; //(a= a - 1)
        
        System.out.println("And then Decrement of a and then value of a :" + a);
        System.out.println("First Use Value of a and then value of b : " + b);
    }
}